// Arreglo de números
const arreglo: number[] = [5, 10, 15, 20, 25, 30, 35, 40, 45, 50];

// Dividir cada valor del arreglo por 5
export const divididoPor5: number[] = arreglo.map(valor => valor / 5);

// Exportar vacío para que el archivo se trate como un módulo
export {};
